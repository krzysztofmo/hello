apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-elixir-deployment
  name: hello-elixir-deployment
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: hello-elixir-deployment
  type: LoadBalancer
